class DiscordLiveStats{constructor(){this.updateInterval = 30000;this.onlineEl = null;this.membersEl = null;this.avatarEls = [];this.init();}init(){const serverStats = document.querySelectorAll( ".server-online,.server-members" );if (serverStats.length > 0){serverStats.forEach((el) =>{if (el.classList.contains("server-online")){this.onlineEl = el;}else if (el.classList.contains("server-members")){this.membersEl = el;}});this.avatarEls = document.querySelectorAll( ".nav-av,.footer-av,.hero-av,[alt*='avatar'],[alt*='Monolith']" );this.updateStats();setInterval(() => this.updateStats(),this.updateInterval);}}async updateStats(){try{const response = await fetch("/.netlify/functions/discord-stats");if (!response.ok){console.warn("Failed to fetch Discord stats:",response.status);return;}const data = await response.json();const{online,members,icon,name}= data;if (this.onlineEl && online > 0){this.onlineEl.innerHTML = `&#x1F7E2;${online}Online`;}if (this.membersEl && members > 0){this.membersEl.innerHTML = `&#x1F465;${members}Members`;}if (icon && this.avatarEls.length > 0){this.avatarEls.forEach((el) =>{if (el.tagName === "IMG"){el.src = icon;el.alt = name || "Monolith Social";el.title = name || "Monolith Social";}});}console.log( `Discord Updated: ${online}online,${members}members,icon: ${icon ? "loaded" : "not available"}` );}catch (error){console.warn("Discord stats error:",error);}}}if (document.readyState === "loading"){document.addEventListener("DOMContentLoaded",() =>{new DiscordLiveStats();});}else{new DiscordLiveStats();}
