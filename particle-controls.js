(function(){const toggle = document.getElementById('particleToggle');const slider = document.getElementById('particleSlider');const intensityValue = document.getElementById('intensityValue');if (!toggle || !slider) return;const savedIntensity = parseInt(localStorage.getItem('particleIntensity') || '100',10);const savedEnabled = localStorage.getItem('particleEnabled') !== 'false';function initializeUI(){slider.value = savedIntensity;intensityValue.textContent = savedIntensity;if (savedEnabled){toggle.classList.add('enabled');}else{toggle.classList.remove('enabled');}updateParticleSystem();}toggle.addEventListener('click',() =>{const isCurrentlyEnabled = toggle.classList.contains('enabled');const newState = !isCurrentlyEnabled;toggle.classList.toggle('enabled');localStorage.setItem('particleEnabled',newState);if (newState){const intensity = parseInt(slider.value,10);if (window.particleSystem){window.particleSystem.setIntensity(intensity);}}else{if (window.particleSystem){window.particleSystem.setIntensity(0);}}});slider.addEventListener('input',(e) =>{const intensity = parseInt(e.target.value,10);intensityValue.textContent = intensity;localStorage.setItem('particleIntensity',intensity);if (window.particleSystem){if (intensity > 0){if (!toggle.classList.contains('enabled')){toggle.classList.add('enabled');localStorage.setItem('particleEnabled','true');}window.particleSystem.setIntensity(intensity);}else{toggle.classList.remove('enabled');localStorage.setItem('particleEnabled','false');if (window.particleSystem){window.particleSystem.setIntensity(0);}}}});function updateParticleSystem(){if (window.particleSystem){if (savedEnabled && savedIntensity > 0){window.particleSystem.setIntensity(savedIntensity);}else{window.particleSystem.setIntensity(0);}}else{setTimeout(updateParticleSystem,100);}}setTimeout(initializeUI,50);})();
